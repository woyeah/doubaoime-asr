# 部署环境配置（含私网 IP / 用户名，不要 commit）
# 用法：cp .env.example .env，按你的 NAS 环境填值

# NAS 私有 Docker registry
REGISTRY=192.168.x.x:5500

# NAS SSH 凭据
NAS_USER=your_nas_user
NAS_HOST=192.168.x.x

# NAS 上的项目目录（首次部署前请手动 git clone 到这里）
NAS_DIR=/volume1/docker/github/doubaoime-asr
